- defaults:
    link:
      create: true
      relink: true
    shell:
      stdout: true
      stderr: true

- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.shell:
    ~/.zsh:
    ~/.zshrc:

- brewfile:
    - Brewfile

- shell:
    - ./macos
    - ./npm

